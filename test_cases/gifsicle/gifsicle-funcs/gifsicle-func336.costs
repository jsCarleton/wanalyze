|f336| = list_max([
  167;
  list_sum([
    183;
    (26 * (N([ln5; pn2], [(pn2 + 1) < ln5], [0; 0 + 1], [ln5; pn2 + 1]))) + 1
  ]);
  list_sum([
    171;
    (26 * (N([ln5; pn2], [(pn2 + 1) < ln5], [0; 0 + 1], [ln5; pn2 + 1]))) + 1
  ])
])
