|f121| = list_max([
  17;
  list_sum([
    18;
    (list_max([
      30 * (N([ln3; pn0], [(pn0 + 1) < ln3], [m[0]; 0], [ln3; pn0 + 1]));
      24 * (N([ln3; pn0], [(pn0 + 1) < ln3], [m[0]; 0], [ln3; pn0 + 1]));
      18 * (N([ln3; pn0], [(pn0 + 1) < ln3], [m[0]; 0], [ln3; pn0 + 1]))
    ])) + 2
  ]);
  list_sum([
    15;
    (list_max([
      30 * (N([ln3; pn0], [(pn0 + 1) < ln3], [m[0]; 0], [ln3; pn0 + 1]));
      24 * (N([ln3; pn0], [(pn0 + 1) < ln3], [m[0]; 0], [ln3; pn0 + 1]));
      18 * (N([ln3; pn0], [(pn0 + 1) < ln3], [m[0]; 0], [ln3; pn0 + 1]))
    ])) + 2
  ])
])
