|f254| = list_max([
  list_sum([
    28;
    (list_max([
      (23 + |f29|) * (N([ln2; pn0], [(pn0 + 1) < ln2], [m[4]; 0], [ln2; pn0 + 1]));
      17 * (N([ln2; pn0], [(pn0 + 1) < ln2], [m[4]; 0], [ln2; pn0 + 1]))
    ])) + 1
  ]);
  list_sum([
    30;
    (list_max([
      (23 + |f29|) * (N([ln2; pn0], [(pn0 + 1) < ln2], [m[4]; 0], [ln2; pn0 + 1]));
      17 * (N([ln2; pn0], [(pn0 + 1) < ln2], [m[4]; 0], [ln2; pn0 + 1]))
    ])) + 1
  ])
])
