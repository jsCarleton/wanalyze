|f350| = list_max([
  82;
  list_sum([
    83;
    (11 * (N([ln6; pn3], [(ln6 * 10) < pn3], [10000 * 10; pn3], [ln6 * 10; pn3]))) + 1
  ]);
  list_sum([
    70;
    (11 * (N([ln6; pn3], [(ln6 * 10) < pn3], [10000 * 10; pn3], [ln6 * 10; pn3]))) + 1
  ])
])
