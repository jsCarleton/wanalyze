|f256| = list_max([
  60;
  list_sum([
    61;
    (list_max([
      30 * (N([ln3; pn2], [(pn2 + 1) < ln3], [m[0]; pn2], [ln3; pn2 + 1]));
      24 * (N([ln3; pn2], [(pn2 + 1) < ln3], [m[0]; pn2], [ln3; pn2 + 1]));
      18 * (N([ln3; pn2], [(pn2 + 1) < ln3], [m[0]; pn2], [ln3; pn2 + 1]))
    ])) + 1
  ]);
  list_sum([
    18;
    (list_max([
      30 * (N([ln3; pn2], [(pn2 + 1) < ln3], [m[0]; pn2], [ln3; pn2 + 1]));
      24 * (N([ln3; pn2], [(pn2 + 1) < ln3], [m[0]; pn2], [ln3; pn2 + 1]));
      18 * (N([ln3; pn2], [(pn2 + 1) < ln3], [m[0]; pn2], [ln3; pn2 + 1]))
    ])) + 1
  ])
])
