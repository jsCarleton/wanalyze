|f96| = list_max([
  20;
  list_sum([
    21;
    (16 * (N([ln2; pn0], [(pn0 + 1) != ln2], [m[4]; 0], [ln2; pn0 + 1]))) + 2
  ]);
  list_sum([
    18;
    (16 * (N([ln2; pn0], [(pn0 + 1) != ln2], [m[4]; 0], [ln2; pn0 + 1]))) + 2
  ])
])
