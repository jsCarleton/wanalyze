|f50| = list_max([
  list_sum(34, 5);
  list_sum(34, 5, 5);
  list_sum([
    34;
    ((11 + |f44|) * (N([pn3], [(pn3 + -256) > 255], [(pn2 - pn3) + -256], [pn3 + -256]))) + 1;
    1;
    5;
    5
  ])
])
