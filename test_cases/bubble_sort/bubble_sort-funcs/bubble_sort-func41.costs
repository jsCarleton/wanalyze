|f41| = list_max([
  list_sum(34, 0);
  list_sum(34, 2);
  list_sum(34, 16, 2);
  list_sum([
    34;
    (14 * (N([m[0]], [m[0] != 10], [m[0]], [m[0]]))) + (list_max(1, 3));
    16;
    2
  ]);
  list_sum([
    34;
    (14 * (N([m[0]], [m[0] != 10], [m[0]], [m[0]]))) + (list_max(1, 3));
    22;
    2
  ]);
  list_sum([
    34;
    (14 * (N([m[0]], [m[0] != 10], [m[0]], [m[0]]))) + (list_max(1, 3));
    22;
    16;
    2
  ])
])
