|f25| = list_max([
  list_sum([
    list_max([
      51 + (list_sum([|f5|; |f41|]));
      54 + (list_sum([|f5|; |f41|]))
    ]);
    1;
    ((63 + (list_sum([|f5|; |f41|]))) * (I([rn0], [i32.eqz(rn0)], [rn0 = rn0], [rn0 = rn0]))) + (list_max([1; 3]));
    1;
    list_max([5; 4]);
    16;
    list_max([23; 17]);
    7
  ]);
  list_sum([
    list_max([
      51 + (list_sum([|f5|; |f41|]));
      54 + (list_sum([|f5|; |f41|]))
    ]);
    1;
    ((63 + (list_sum([|f5|; |f41|]))) * (I([rn0], [i32.eqz(rn0)], [rn0 = rn0], [rn0 = rn0]))) + (list_max([1; 3]));
    1;
    list_max([5; 4]);
    16;
    7
  ]);
  list_sum([
    list_max([
      51 + (list_sum([|f5|; |f41|]));
      54 + (list_sum([|f5|; |f41|]))
    ]);
    1;
    ((63 + (list_sum([|f5|; |f41|]))) * (I([rn0], [i32.eqz(rn0)], [rn0 = rn0], [rn0 = rn0]))) + (list_max([1; 3]));
    1;
    list_max([5; 4]);
    list_max([23; 17]);
    7
  ]);
  list_sum([
    list_max([
      51 + (list_sum([|f5|; |f41|]));
      54 + (list_sum([|f5|; |f41|]))
    ]);
    1;
    ((63 + (list_sum([|f5|; |f41|]))) * (I([rn0], [i32.eqz(rn0)], [rn0 = rn0], [rn0 = rn0]))) + (list_max([1; 3]));
    16;
    list_max([23; 17]);
    7
  ]);
  list_sum([
    list_max([
      51 + (list_sum([|f5|; |f41|]));
      54 + (list_sum([|f5|; |f41|]))
    ]);
    1;
    ((63 + (list_sum([|f5|; |f41|]))) * (I([rn0], [i32.eqz(rn0)], [rn0 = rn0], [rn0 = rn0]))) + (list_max([1; 3]));
    16;
    7
  ]);
  list_sum([
    list_max([
      51 + (list_sum([|f5|; |f41|]));
      54 + (list_sum([|f5|; |f41|]))
    ]);
    1;
    ((63 + (list_sum([|f5|; |f41|]))) * (I([rn0], [i32.eqz(rn0)], [rn0 = rn0], [rn0 = rn0]))) + (list_max([1; 3]));
    list_max([23; 17]);
    7
  ]);
  list_sum([
    list_max([
      51 + (list_sum([|f5|; |f41|]));
      54 + (list_sum([|f5|; |f41|]))
    ]);
    list_max([5; 4]);
    16;
    list_max([23; 17]);
    7
  ]);
  list_sum([
    list_max([
      51 + (list_sum([|f5|; |f41|]));
      54 + (list_sum([|f5|; |f41|]))
    ]);
    list_max([5; 4]);
    16;
    7
  ]);
  list_sum([
    list_max([
      51 + (list_sum([|f5|; |f41|]));
      54 + (list_sum([|f5|; |f41|]))
    ]);
    list_max([5; 4]);
    list_max([23; 17]);
    7
  ])
])
