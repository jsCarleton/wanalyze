|f9| = list_max([
  list_sum([
    57 + (list_sum([|f8|; |f29|]));
    12 + |f8|;
    ((11 + |f8|) * (I([pn2], [(pn2 - 256) > 255], [pn2 = pn2 - pn3], [pn2 = pn2 - 256]))) + 1;
    6 + |f8|;
    10 + |f8|;
    5
  ]);
  list_sum([
    57 + (list_sum([|f8|; |f29|]));
    10 + |f8|;
    5
  ]);
  list_sum([
    57 + (list_sum([|f8|; |f29|]));
    5
  ])
])
